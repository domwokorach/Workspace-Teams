/**
 * Placeholder for CRDT-backed collaborative editing. Not wired up yet — no
 * `yjs` dependency is installed. `document-store.ts` documents the same
 * contract with a last-write-wins in-memory store so callers don't need to
 * change when this is implemented for real.
 */
export {};
